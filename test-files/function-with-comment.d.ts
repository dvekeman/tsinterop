declare module "blah" {
/**
 *
 *  Clear the modal prompt / being shown to the user.
 */
  function clearPrompt(): void;

  /**
   *
   *  Clear the modal prompt / being shown to the user. */
  function clearPrompt2(): void;

  /** Clear the modal prompt / being shown to the user. */
  function clearPrompt3(): void;

  /**
   *  Clear the modal prompt / being shown to the user.
   */
  function clearPrompt4(): void;
}